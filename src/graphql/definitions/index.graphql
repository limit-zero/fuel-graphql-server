# import *, *.Query from 'data-extension.graphql'
# import *, *.Query from 'data-folder.graphql'

directive @booleanValue(localField: String) on FIELD_DEFINITION

type Query {
  ping: String!
}

input ObjectIDInput {
  id: ID!
}

input IntegerIDInput {
  id: Int!
}
